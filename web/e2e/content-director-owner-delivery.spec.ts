/**
 * Superseded by PROJECT-COMMAND-CENTER-CANONICAL-01.
 * Keep file as a pointer so old CI invocations fail clearly instead of asserting stage-nav.
 * Canonical walkthrough: e2e/project-command-center-canonical.spec.ts
 */
import { test } from "@playwright/test";

test.describe.skip(
  "PROGRAM-CONTENT-01-UX-INTEGRATION-01-REGRESSION-FIX",
  "Superseded by PROJECT-COMMAND-CENTER-CANONICAL-01",
  () => {
    test("see project-command-center-canonical.spec.ts", async () => {
      // intentionally skipped
    });
  },
);
