import { DashboardView } from "@/components/views/dashboard-view";

/** Legacy internal operations dashboard — not the public commercial landing. */
export default function InternalDashboardPage() {
  return <DashboardView />;
}
