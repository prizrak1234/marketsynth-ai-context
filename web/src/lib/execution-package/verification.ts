/**
 * Stub modules for verification/rollback helpers (logic lives in build-package).
 * Kept as separate files per A7 structure contract.
 */

export { buildExecutionPackage } from "@/lib/execution-package/build-package";
