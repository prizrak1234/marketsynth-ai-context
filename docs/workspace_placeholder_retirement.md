# Workspace placeholder retirement

## Removed from primary nav destinations

`WorkspacePlaceholder` (“Product Alpha… Backend не подключён”) is no longer used by:

- investigations, research (redirect), verdicts, strategies, execution (redirect),
  implementation, assets, knowledge, settings, tasks.

## Still present

Component file retained for rare legacy use — not linked from primary IA.

## Copy banned on section pages

- Product Alpha reservation
- “Backend не подключён” as blanket claim
- Fake proj_alpha / mock verdict cards in backend mode
