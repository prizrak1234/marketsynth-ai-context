# Commercial MVP P1.1 — Dependencies and Roles

## Dependencies

Typed edges with cycle detection (`dependency_cycle`). Blocking deps feed readiness. Not MarketingPlan task order.

## Roles

`ImplRoleAssignment` with `role_type` including unsupported.  
`execution_mapping_allowed=false` for unsupported. **No new AgentType.**
