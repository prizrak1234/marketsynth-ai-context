# Commercial MVP P0.4 — Evidence–Source linkage

`EvidenceSourceLink`: stance (`supports`/`contradicts`/`context`), optional locator + bounded excerpt.

- Exact immutable Source version by id
- Same Project/owner
- Source archived/rejected/superseded rejected
- Context-only cannot confirm Evidence on accept
- Conflicting Evidence keeps both supports and contradicts links
