# Home USP — differentiated value proposition

Authenticated Home (`/workspace`) shows three equal cards after the offer/support, before the work question.

| Card | Mechanism |
|------|-----------|
| Не рекламируем слабые идеи | Viability gate before ad spend |
| Собираем команду под конкретную задачу | Route to specialist vs full cycle |
| Показываем, на чём основан вывод | Sources / evidence / assumptions — no guess as fact |

Above cards: economic value line (`home.usp.economicValue`).  
Below cards: owner-control line (`home.usp.controlLine`).

Copy lives in `web/src/lib/i18n/translations/{ru,en}.ts` only.  
Does not promise guaranteed profit or zero losses.  
Does not use Evidence / Runtime / SoT / MarketingPlan in user copy.
