# Role & Objective
Act as a Senior UI/UX Designer and Frontend Developer. Your goal is to convert the attached reference image (or text description) into a pixel-perfect HTML/CSS module. 

# Design & Integration Instructions
1. **Modularity:** Ensure this new section is 100% independent. Do not nest it inside the `<header>` or `<footer>`. It must be placed inside the existing `<main>` tag.
2. **Visual Constraints:** - Extract Hex codes for background and text to match the reference.
   - Use CSS Flexbox and Grid for the internal layout of this module.
   - Ensure the design is fully responsive (mobile-first).
3. **Typography & Assets:** Guess the font family or use a close Google Fonts alternative (Inter, Roboto, Poppins). For UI elements, strictly use Iconify Solar Linear icons.
4. **Output:** Provide ONLY the HTML, CSS, and JS specific to this new section. Do not rewrite the entire page architecture.
